---
wave: 1
depends_on: []
files_modified: ["styles.css", "index.html", "blog/index.html", "script.js"]
autonomous: true
---

# Plan 02-01: CRO & Lead Quality Enhancements

## Goal
Optimize the website to capture high-intent real estate investors through frictionless direct-communication channels (WhatsApp / Calls) and premium copy.

## Requirements
- CRO-01: Persistent, highly visible "WhatsApp for Brochure" and "Call Now" buttons for immediate mobile intent.
- CRO-02: Optimize hero copy to highlight appreciation, capital gains, and premium positioning.
- CRO-03: Make sure forms redirect to WhatsApp seamlessly if Google Apps Script fails.
- LEAD-01: Refine copy to filter for serious buyers (emphasize premium nature).

## Tasks

<task>
<action>
In `styles.css`, update the `.sticky-cta` class to remove `display: none` natively, and wrap its visibility inside an `@media (max-width: 768px)` query so it only shows on mobile devices. Adjust padding/blur for a native app feel.
</action>
<read_first>
- `styles.css`
</read_first>
<acceptance_criteria>
- `.sticky-cta` is governed by a mobile media query, showing `display: flex`.
</acceptance_criteria>
</task>

<task>
<action>
In `index.html`, revise the hero section `<p>` paragraph to loudly emphasize "Premium RERA-registered investment opportunity", bolding "15-20% annual capital appreciation", and asserting a premium tone.
</action>
<read_first>
- `index.html`
</read_first>
<acceptance_criteria>
- `index.html` hero contains the string "Premium RERA-registered".
</acceptance_criteria>
</task>

<task>
<action>
In `script.js`, modify the `catch (error)` routines inside BOTH the `contactForm` and `quickForm` submission blocks to execute `window.open(...)` with the pre-assembled `whatsappMsg`, ensuring zero leads are lost due to API failures.
</action>
<read_first>
- `script.js`
</read_first>
<acceptance_criteria>
- `script.js` error blocks include a `window.open` fallback for WhatsApp.
</acceptance_criteria>
</task>

## Verification
### Must Haves
1. Mobile view displays sticky CTAs at the bottom.
2. Forms immediately rescue failed Google Script pings to WhatsApp.
### Verification Steps
1. Visual inspection using mobile viewport resolution.
2. Search codebase for the updated `.sticky-cta` media query.
